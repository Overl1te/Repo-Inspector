services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - sqlite_data:/app/data
      - ./config.yml:/app/config.yml:ro

volumes:
  sqlite_data:
